import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  installmentsHeader: {
    paddingHorizontal: 0,
    paddingTop: 32,
    paddingBottom: 16,
  },

  installmentsTitle: {
    fontSize: 18,
    fontWeight: '800',
    color: '#1565C0',
    textAlign: 'left',
  },
});
